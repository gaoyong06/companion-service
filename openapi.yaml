# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: Companion API
    version: 0.0.1
paths:
    /companion/v1/audio-messages:
        post:
            tags:
                - Companion
            operationId: Companion_SendAudioMessage
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SendAudioMessageRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SendAudioMessageReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /companion/v1/memory-feedback:
        post:
            tags:
                - Companion
            operationId: Companion_SubmitMemoryFeedback
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/MemoryFeedbackRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/MemoryFeedbackReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /companion/v1/messages:
        post:
            tags:
                - Companion
            operationId: Companion_SendMessage
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/SendMessageRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SendMessageReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                            $ref: '#/components/schemas/Status'
    /companion/v1/media-messages:
        post:
            tags:
                - Companion
            operationId: Companion_SendMediaMessageMultipart
            requestBody:
                required: true
                content:
                    multipart/form-data:
                        schema:
                            type: object
                            required:
                                - file
                            properties:
                                file:
                                    type: string
                                    format: binary
                                caption:
                                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/SendMediaMessageReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /companion/v1/timeline:
        get:
            tags:
                - Companion
            operationId: Companion_GetTimeline
            parameters:
                - name: limit
                  in: query
                  schema:
                    type: integer
                    format: int32
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/TimelineReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        ConversationMessage:
            type: object
            properties:
                messageId:
                    type: string
                role:
                    type: string
                content:
                    type: string
                createdAt:
                    type: string
                assets:
                    type: array
                    items:
                        $ref: '#/components/schemas/MessageAsset'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        MemoryFeedbackReply:
            type: object
            properties:
                accepted:
                    type: boolean
        MemoryFeedbackRequest:
            type: object
            properties:
                messageId:
                    type: string
                action:
                    type: string
                kind:
                    type: string
                content:
                    type: string
        MessageAsset:
            type: object
            properties:
                messageAssetId:
                    type: string
                assetId:
                    type: string
                mediaType:
                    type: string
                contentType:
                    type: string
                filename:
                    type: string
                url:
                    type: string
                sizeBytes:
                    type: string
        SendAudioMessageReply:
            type: object
            properties:
                message:
                    $ref: '#/components/schemas/SendMessageReply'
                audioData:
                    type: string
                    format: bytes
                audioContentType:
                    type: string
        SendAudioMessageRequest:
            type: object
            properties:
                audioData:
                    type: string
                    format: bytes
                filename:
                    type: string
                contentType:
                    type: string
                language:
                    type: string
                voice:
                    type: string
                synthesize:
                    type: boolean
        SendMessageReply:
            type: object
            properties:
                userMessage:
                    $ref: '#/components/schemas/ConversationMessage'
                assistantMessage:
                    $ref: '#/components/schemas/ConversationMessage'
        SendMessageRequest:
            type: object
            properties:
                content:
                    type: string
        SendMediaMessageReply:
            type: object
            properties:
                message:
                    $ref: '#/components/schemas/SendMessageReply'
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        TimelineReply:
            type: object
            properties:
                messages:
                    type: array
                    items:
                        $ref: '#/components/schemas/ConversationMessage'
tags:
    - name: Companion
